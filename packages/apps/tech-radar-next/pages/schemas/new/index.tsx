/* eslint-disable-next-line */
import SchemaEditor from '../../../components/schemaEditor';

export interface NewProps {}

export function NewSchemaPage(props: NewProps) {
  return <SchemaEditor />;
}

export default NewSchemaPage;
